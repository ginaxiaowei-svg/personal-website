# Codex Working Guide

本文件是这个项目的长期执行约定。

目标：
- 在保持当前视觉气质稳定的前提下，持续迭代个人网站
- 保证首页、作品页、样式系统和内容结构不失控
- 让每次修改都能快速定位文件、明确边界、降低返工

执行规则：
- 每次开始修改前，先阅读本文件
- 先理解现有结构，再改动页面
- 优先复用已有样式和页面骨架，不随意新增平行体系
- 所有内容默认中文优先
- 不做对参考站点的 1:1 复制，保持“高相似气质 + 原创表达”

## 1. 当前架构

项目当前是轻量 Node 本地站点，带简单后台，没有数据库，也没有复杂构建步骤。

当前结构：
- `server.js`
  站点入口，负责前台渲染、后台页面、内容 API、静态资源分发
- `data/site-content.json`
  站点内容单一数据源，首页和作品页都从这里读取
- `styles.css`
  全站共享样式，包括首页、case study 页和后台页
- `assets/`
  本地素材目录
- `.figma-cache/`
  设计参考与临时缓存，不作为正式网页代码依赖

当前页面关系：
- 首页是流量入口
- 首页的每个作品卡片必须跳转到一个独立的 `/work/:slug`
- 二级页统一使用同一套 case study 骨架
- 后台页面位于 `/admin`

## 2. 页面职责

### 首页 `/`

首页负责：
- 展示名字、身份定位、自我介绍
- 展示履历时间线
- 展示作品列表
- 提供“实验 / 关于 / 联系方式”补充信息

首页不负责：
- 承载完整 case study 长文
- 承载大量逐屏叙事内容
- 承载复杂数据逻辑

首页原则：
- 维持克制、留白、编辑感
- 作品卡片只承担“引导点击”的职责
- 作品卡片信息要短，不在首页写成长文

### 二级页 `/work/:slug`

二级页负责：
- 完整讲清单个项目
- 使用 case study 结构进行叙事
- 承载项目背景、问题、过程、结果、复盘

二级页默认结构：
1. Hero
2. Meta 信息
3. Cover / 头图区域
4. Background
5. Challenge
6. Process / Solution
7. Result
8. Reflection

如果后续项目不同，不强行套死，但必须保留“结构化阅读体验”。

## 3. 样式系统约定

样式全部集中在 `styles.css`。

原则：
- 优先扩展已有 token 和已有组件类
- 能复用就不要复制一整段样式
- 首页样式和二级页样式可以共存，但要按模块分区书写

当前样式层级建议：
1. Root tokens
2. Global reset
3. Shared layout
4. Header / footer
5. Homepage modules
6. Project card modules
7. Case study modules
8. Responsive rules
9. Reduced motion

禁止事项：
- 不要随意新增第二份样式文件，除非明确拆分为多文件架构
- 不要给单个页面堆很多内联样式
- 不要为了修一个页面而破坏全站 token

## 4. 内容维护规则

当前内容统一维护在 `data/site-content.json`。

内容位置：
- 首页个人信息：`data/site-content.json > site`
- 首页履历：`data/site-content.json > experiences`
- 首页社交链接：`data/site-content.json > socialLinks`
- 作品卡片和 case study：`data/site-content.json > works`

后台维护入口：
- 浏览器访问 `/admin`
- 后台保存时会直接写回 `data/site-content.json`

后续如果数据继续增多，再考虑拆分成多个 JSON 文件或接入数据库。

## 5. 命名约定

文件命名：
- 作品页文件名使用英文 kebab-case
- 不使用中文文件名作为正式页面路径

类名命名：
- 使用语义化英文类名
- 避免临时类名如 `.box1` `.tmp` `.left-part`
- 页面专属模块可以使用 `case-*`、`project-*`、`hero-*` 等前缀

文案约定：
- 页面文案默认中文
- 英文只作为补充，不抢主叙事
- 标题尽量短而清晰，避免 AI 味太重的空泛表达

## 6. 改动策略

每次改动时遵循：

### 小改动

适用于：
- 替换文案
- 改链接
- 改单个模块布局
- 增减一个作品页

做法：
- 优先改 `data/site-content.json`
- 如果样式可复用，尽量不新增太多 CSS
- 修改后检查首页和对应二级页是否都正常

### 中改动

适用于：
- 首页结构调整
- 作品卡片系统调整
- case study 模板升级

做法：
- 先确认会影响哪些页面
- 统一修改共享样式
- 至少抽查一个首页和两个二级页

### 大改动

适用于：
- 改成数据驱动
- 引入模板系统
- 引入构建工具
- 引入后台或 CMS

做法：
- 先写迁移方案
- 保证能平滑回退
- 不要在没有确认的情况下直接重构全部页面

## 7. Case Study 写作模板

后续补项目内容时，优先按这个结构写：

### A. 项目概览
- 项目名称
- 时间
- 角色
- 项目类型
- 一句话总结

### B. 背景
- 为什么要做
- 服务谁
- 当前问题是什么

### C. 目标
- 业务目标
- 用户目标
- 设计目标

### D. 挑战
- 复杂性来源
- 限制条件
- 为什么难

### E. 过程
- 调研
- 分析
- 关键决策
- 方案迭代

### F. 结果
- 上线结果
- 数据反馈
- 业务影响

### G. 复盘
- 做对了什么
- 没做好的地方
- 下次怎么优化

## 8. 视觉一致性要求

需要始终保持：
- 大标题有辨识度
- 留白足够
- 边框和层级克制
- 项目卡片比例统一
- 首页与二级页视觉语言一致

避免：
- 颜色系统失控
- 组件风格混杂
- 某个页面突然很花，另一个页面极简
- 首页太满，像资讯列表
- 二级页像文档页，没有作品集的气质

## 9. 本地运行与验收

本地启动方式：

```bash
npm start
```

预览地址：

```text
http://127.0.0.1:4321/
```

每次改完至少检查：
- 首页能打开
- 一个二级页能打开
- 后台 `/admin` 能打开
- 后台保存后前台能读到新内容
- 首页作品卡片跳转正常
- CSS 没有把移动端挤坏
- 中文文案没有明显折行问题

## 10. 每次执行前必读清单

开始改之前，按这个顺序：

1. 先阅读 `codex.md`
2. 确认本次改动影响首页、二级页还是样式系统
3. 找到会被修改的具体文件
4. 尽量复用已有结构，不重复造轮子
5. 改完后本地启动并验证入口页面

## 11. 后续推荐演进路线

按优先级建议：

### Phase 1
- 把首页和 8 个 case study 的真实内容补全
- 替换占位社交链接
- 替换占位图或项目封面

### Phase 2
- 把作品列表抽到单独数据文件
- 统一 case study 元信息结构
- 增加 SEO 基础信息

### Phase 3
- 视需要接入轻量 CMS 或管理后台
- 支持更方便的内容更新
- 支持博客 / 笔记 / 实验内容模块

## 12. 最重要的判断标准

如果拿不准该怎么改，优先选择：
- 更简单的结构
- 更清晰的内容层级
- 更少但更稳定的样式
- 更容易被下一次维护理解的实现

这个项目的重点不是“炫技”，而是“长期可维护的高质量个人网站”。
